
import classNames from 'classnames/bind'
import styles from './FilterBox.module.scss'

const cx = classNames.bind(styles)

const FilterBox = () => {
  return <div className={cx('filter-box')}>FilterBox</div>
}

export { FilterBox }
